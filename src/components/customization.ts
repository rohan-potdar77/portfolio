import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({
  palette: {
    background: { default: "#2C2F30" },
    primary: { main: "#F16645" },
    secondary: { main: "#FFC65D" },
    success: { main: "#4CC3D9" },
  },
});
